╔══════════════════════════════════════════════════════════════╗
║           Todo Chatbot - Deployment Successful               ║
╚══════════════════════════════════════════════════════════════╝

Release: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}
Environment: {{ .Values.global.environment }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📦 DEPLOYMENT STATUS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Check the status of your pods:

  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

Watch pods come online:

  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }} --watch

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🌐 ACCESS YOUR APPLICATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

{{- if .Values.frontend.enabled }}
Frontend UI:
{{- if eq .Values.frontend.service.type "NodePort" }}
  http://$(minikube ip):{{ .Values.frontend.service.nodePort }}

Or use Minikube service directly:
  minikube service frontend-service -n {{ .Release.Namespace }}
{{- else }}
  kubectl port-forward -n {{ .Release.Namespace }} svc/frontend-service {{ .Values.frontend.service.port }}:{{ .Values.frontend.service.port }}
  Then access: http://localhost:{{ .Values.frontend.service.port }}
{{- end }}
{{- end }}

{{- if .Values.backend.enabled }}
Backend API:
  kubectl port-forward -n {{ .Release.Namespace }} svc/backend-service {{ .Values.backend.service.port }}:{{ .Values.backend.service.port }}
  Then access: http://localhost:{{ .Values.backend.service.port }}/docs
{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔐 SECRETS CONFIGURATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

{{- if not .Values.secrets.create }}
⚠️  Secrets are configured for manual creation.

Create the required secret:

  kubectl create secret generic {{ .Values.secrets.name }} \
    --from-literal=DATABASE_URL="your-database-url" \
    --from-literal=OPENAI_API_KEY="your-openai-key" \
    --from-literal=BETTER_AUTH_SECRET="your-auth-secret" \
    -n {{ .Release.Namespace }}

Then restart the backend deployment:

  kubectl rollout restart deployment/backend -n {{ .Release.Namespace }}
{{- else }}
✓ Secrets created automatically from values
{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🤖 AI OPS INTEGRATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

This deployment is compatible with kubectl-ai and kagent.

Query your deployment with kagent:

  kagent "What is the status of the todo app?"
  kagent "Show me the backend logs"
  kagent "Scale the backend to 3 replicas"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 MONITORING & DEBUGGING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

View logs:
  kubectl logs -n {{ .Release.Namespace }} -l tier=backend --tail=50 -f
  kubectl logs -n {{ .Release.Namespace }} -l tier=frontend --tail=50 -f

Check resource usage:
  kubectl top pods -n {{ .Release.Namespace }}

Describe a pod:
  kubectl describe pod -n {{ .Release.Namespace }} <pod-name>

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔄 UPGRADE & ROLLBACK
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Upgrade this release:
  helm upgrade {{ .Release.Name }} ./infrastructure/helm/todo-chart -n {{ .Release.Namespace }}

Rollback to previous version:
  helm rollback {{ .Release.Name }} -n {{ .Release.Namespace }}

View release history:
  helm history {{ .Release.Name }} -n {{ .Release.Namespace }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📚 DOCUMENTATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Specification: specs/007-minikube-deployment/spec.md
Implementation Plan: specs/007-minikube-deployment/plan.md
Quick Start Guide: specs/007-minikube-deployment/quickstart.md

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Happy hacking! 🚀
